name: Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: enonic/action-app-build@v2
      
      - uses: codecov/codecov-action@v6
